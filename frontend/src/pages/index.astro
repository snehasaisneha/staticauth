---
import Layout from '../layouts/Layout.astro';
import { HomePage } from '@/components/HomePage';

const appName = import.meta.env.PUBLIC_APP_NAME || 'Gatekeeper';
---

<Layout title="Home">
  <HomePage client:load appName={appName} />
</Layout>
