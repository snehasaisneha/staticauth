---
import Layout from '../../layouts/Layout.astro';
import { AdminPage } from '@/components/admin/AdminPage';

const appName = import.meta.env.PUBLIC_APP_NAME || 'Gatekeeper';
---

<Layout title="Admin">
  <AdminPage client:load appName={appName} />
</Layout>
